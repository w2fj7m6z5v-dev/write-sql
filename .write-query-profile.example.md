# write-query 本地使用者权限配置
#
# 复制本文件为仓库根目录 `.write-query-profile.md` 后按实际身份修改。
# `.write-query-profile.md` 已被 Git 忽略，不会提交或影响其他克隆仓库。
#
# 默认不用配置 `access_scope`：使用 Excel "业支 Hive 原表"。
# 分局：access_scope: subst；必须填写 subst-code。
# 销售部/政企部：access_scope: sales。

# access_scope:
# subst-code:

# 分局简称仅在 access_scope: subst 时填写：
# th=天河、py=番禺、ns=南沙、ch=从化、hd=花都、hp=黄埔、
# lw=荔湾、yx=越秀、zc=增城、by=白云、hz=海珠。
