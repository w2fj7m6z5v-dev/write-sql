# 主宽入网量按字段汇总 — 交互记录

> 日期：2026-06-11  
> 技能：/write-query

---

**用户：** 提取主宽入网量。按字段汇总，字段：月份，分局，营服，揽装人，揽装分局，经营主体，出账收入

---

**Claude：** 我先按这个理解梳理一下，有几个会改变 SQL 的关键缺口需要确认：

我的理解：
- 查什么：主宽入网量（计数），同时汇总出账收入（SUM）
- 查谁：主宽带用户
- 结果：按 月份、分局、营服、揽装人、揽装分局、经营主体、出账收入 分组汇总
- 默认假设：
  - "入网"按竣工时间统计（CDAP 常见口径，以竣工时间为准）
  - "主宽"指主宽带产品（公众/家庭宽带）
  - 出账收入为月度账单收入汇总

还需要确认的 3 个关键点：
1. 时间范围？— 查哪个月或哪几个月的？例如最近完整账期（2026年5月），还是某段范围？
2. "入网"口径？— 按受理时间还是竣工时间算入网？"主宽"是否有特定产品范围（如排除团单/商务/特定低档套餐）？
3. "出账收入"的归属口径？— 出账收入是按入网当月的收入，还是按最近完整账期的到达收入？这对数据结果影响很大。

请补充以上信息。

---

**用户：** 202605月份，按竣工时间，入网当月收入

---

**Claude：** 好的，明确了。让我进入主表选择阶段，先查路由信息。

[加载 ROUTING.md]

好的，信息基本清晰了。让我整理方案确认。

方案确认

我的理解方案如下：
- 主表：069 全业务资料表 dwm_yz_tb_comm_cm_all_final（2026年5月，近半年账期用日表）
- 入网口径：is_new_user=1 + prod_type=40（宽带）+ kd_desc='普通宽带' + 竣工时间=202605
- 入网量：COUNT(serv_id)
- 出账收入：SUM(fee) — 入网当月该号码的出账收入公允后金额
- 分组：月份、分局（subst_name）、营服（branch_name）、揽装人（sales_name）、揽装分局、经营主体
- 补表：⚠️ 涉及「揽装分局」「经营主体」两个字段需补表

还有 2 个确认点：
1. 竣工时间字段 — 您说的"按竣工时间"，069 中有两个候选字段：
   - open_date（服务开通时间）— 标准指标 M-BASIC-BB-001 使用此字段
   - subs_stat_date（入网订单状态日期）— 对应订单竣工时间
   请问用哪个？还是两者在您这边是一回事？
2. 出账收入字段 — fee（出账收入公允后）是 069 的月度出账金额。是否就是这个口径？

请确认以上方案，我再展开补表细节。

---

**用户：** 1、按open_date  2 是的

---

**Claude：** 好的，已确认！现在补表和字段映射阶段，涉及「揽装分局」和「经营主体」两个字段需要补表。让我查看补表规则。

[加载 FIELD_BACKFILL.md、112_网点维表.md]

现在信息齐全了。我来输出完整的方案确认。

方案确认

主表：
- 主表：069 全业务资料表（日表）dwm_yz_tb_comm_cm_all_final
- 分区：par_month_id = '202605'
- 入网口径：is_new_user=1 + prod_type=40（宽带）+ kd_desc='普通宽带' + date_format(open_date,'yyyyMM')='202605'

字段映射：
- 月份 → 069 自带 par_month_id
- 分局 → 069 自带 subst_name（划小分局名称）
- 营服 → 069 自带 branch_name（划小营服名称）
- 揽装人 → 069 自带 sales_name
- 揽装分局 → 069 自带 channel_subst_name（网点所属区县）
- 经营主体 → 需补 112 网点维表 own_operators_name
- 出账收入 → 069 自带 SUM(fee)

补表方案：
- 经营主体 → 112 网点维表 zone_gz_yz.dwd_yz_sale_outlers_final（日表）
- JOIN 键：069.channel_nbr = 112.channel_nbr
- 粒度：网点级唯一
- 行数风险：一对一，无放大风险

待确认项：
1. 揽装分局 — 我默认 069 的 channel_subst_name（网点所属区县）即为「揽装分局」，是否准确？
2. 经营主体 — 补 112 表 own_operators_name，是否有其他口径要求？

请确认，我即生成完整 SQL 并审计。

---

**用户：** 这里为啥用112 不用113？

---

**Claude：** 好问题，我查一下 113 表的结构来核实。

[加载 113_揽装所属表.md]

你说得对，我纠正。069 表文档明确写了：

> 主体编码/主体名称：069 不直接取；固定补 113 揽装所属表的 own_operators_nbr、own_operators_name，优先按 channel_nbr 关联

113 表文档也指出了同样的用法。我先前错误地引用了 FIELD_BACKFILL.md 的 112 方案，但按冲突优先级（表文档 > FIELD_BACKFILL），应以 113 为准。

补充说明：113 表是「有效网点 × 有效揽装人」对应关系，一个网点下可能有多个揽装人（多行），所以按 channel_nbr 关联时需要先去重，避免行数被放大。

修正后的补表方案：
- 经营主体（编码+名称） → 113 揽装所属表（日表）zone_gz_yz.dwd_yz_sales_man_outlers_final
- JOIN 键：069.channel_nbr = 113.channel_nbr
- 去重方式：ROW_NUMBER() OVER (PARTITION BY channel_nbr ORDER BY staff_id) = 1
- 行数风险：一网点多揽装人，需去重后一对一

其他字段无需改动，确认后我即生成完整 SQL。

还有其他疑问吗？

---

**用户：** 没有了

---

**Claude：** 好，进入 SQL 生成阶段。先读取审计规则。

[加载 RULES.md]

现在生成完整 SQL 和审计。

[完整 SQL、审计结果、自检 SQL 已交付]

---

**用户：** 和你交互过程 你生成一份md文档，放在当前目录下

---

**Claude：** [生成了 202605_主宽入网量按字段汇总.md，结构化的技术文档]

---

**用户：** 我是让你吧我和你交付的记录，相当于微信那种聊天记录保存 不是要你整理在保存，先澄清我的需求

---

**Claude：** 明白了，你要的是原始对话记录（聊天记录），不是结构化的技术文档。我重新生成。

[生成本文件]
